---
dependency:
  name: galaxy
driver:
  name: docker
docker:
  containers:
    - name: ansible-playbooks
      image: ubuntu
      image_version: latest
      ansible_groups:
        - group1
    - name: ansible-playbook2
      image: centos
      image_version: latest
      ansible_groups:
        - group2

verifier:
  name: testinfra
